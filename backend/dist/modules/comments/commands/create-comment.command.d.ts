export declare class CreateCommentCommand {
    readonly content: string;
    readonly postId: string;
    readonly authorId: string;
    constructor(content: string, postId: string, authorId: string);
}
